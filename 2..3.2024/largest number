#include <stdio.h>
int main(){
int a=10,b=5,c=20;
int max = a;
if (max<b){
    max=b;
}
if (max<c){
    max=c;
}
printf("the maximum number among the threaa numbers is %d",max);
}



o/p:
the maximum number among the threaa numbers is 20
