#include <stdio.h>
#include <math.h>

int main()
{
 int a,b,temp;
 printf("enter the values of a and b:");
 scanf("%d%d",&a,&b);
 temp=a;
 a=b;
 b=temp;
 printf("the swapped number is %d %d",a,b);

}


o/p:
enter the values of a and b:20 30
the swapped number is 30 20
