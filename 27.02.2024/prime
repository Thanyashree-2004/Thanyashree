#include <stdio.h>
#include <math.h>

void prime(int n){
    int flag=1;
    for(int i=2;i<n/2;i++){
        if (n%i==0){
            flag=0;
            break;            
        }
    }
    if(flag){
        printf("the number is prime");
    }
    else{
        printf("the number is not prime");
    }
}
int main(){
    int n;
    scanf("%d",&n);
    prime(n);
}



o/p:
2
the number is prime
